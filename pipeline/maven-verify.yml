platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: latest

inputs:
  - name: git-repo
  - name: changed-projects

run:
  path: practice-objectives/FY18-Q2/monorepo/pipeline/verify.sh
  dir: git-repo
